{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          { "type": "command", "command": "python -m docbase sync --quiet || python3 -m docbase sync --quiet", "timeout": 180 }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          { "type": "command", "command": "python -m docbase sync --quiet || python3 -m docbase sync --quiet", "timeout": 180 }
        ]
      }
    ]
  }
}
